# Research-loop health — 2026-09-12

## Operational verdict

The critical path is usable in manual, evidence-gated mode. The automatic
staging loop remains paused because its selected item `H-MY-TASK1` is stale
and underspecified relative to the canonical Grand MCA frontier; it must not
be activated blindly.

## Model/tool checks

- Leanstral ad-hoc: **PASS**. The local launcher
  `staging-lean-setup/tools/leanstral-ad-hoc.cmd` reached model
  `mistral/labs-leanstral-1-5-1` through the configured local
  OpenAI-compatible endpoint and returned a verified Lean proof on a smoke
  theorem. It is for isolated snippets only and has no Aristotle project
  context.
- Aristotle API: **NOT CONFIRMED**. `ARISTOTLE_API_KEY` is present in the
  environment, but no `ARISTOTLE_BASE_URL` and no local Aristotle API client
  were found. A key alone is not evidence of a working transport, so no
  request was sent and no API success is claimed.
- Aristotle cloud reports remain `REPORTED_NOT_REPLAYED` until the sources are
  delivered and replayed in the canonical Lean project.

## Current research routing

- Codex: integrator, adversarial mathematical audit, exact arithmetic,
  canonical GitHub state and acceptance gate.
- Agent2: narrow collision/second-moment and finite-field computations only;
  do not use average fibre size, affine dimension, or unproved periodicity as
  maximum-fibre bounds.
- Aristotle(parallel): SAFE heavy-point structure after the reported
  decisive reduction; do not reopen unconstrained annihilators or dead local
  counting routes.
- Aristotle(new): formalize only a Codex-accepted quantitative theorem or a
  complete official counterexample.
- Leanstral: cheap local proof/type-check assistant for small standalone Lean
  lemmas, never evidence for the cloud project by itself.

## Canonical next gates

1. UNSAFE: exact maximum fixed-weight MatchedFamily fibre, using collision or
   second-moment identities and explicit finite-field tests.
2. SAFE: structure forced by a point incident with more than
   `128360144567623878` official challenges.

The H8-invariant family, generic kernel/annihilator, dyadic-only, glue-pencil,
pivot-descent, generic Johnson, and averaging-only routes stay closed.
