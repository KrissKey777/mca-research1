# GRAND MCA — dual gate package v2

## Shared rules

Frozen target: n=2097152, k=1048576, e=978944, t=1118208, w=69632,
p=2130706433, B*=274980728111395087. Replay existing adapters and arithmetic
before use. Do not claim universal safety or failure from a partial branch.

## Aristotle(new): remaining MatchedFamily pointwise gate

The exact additive map, Fourier inversion, full antipodal pairing, and the
sector a1=a3=a5=0 with odd label frequency are complete; that sector is below
the residual pointwise budget by factor 455874015. The generic finite-field
moment-subset-sum theorem is quantitatively useless here because its degree-six
constant is about 2.3051e12 for a 255-point domain.

Resolve the complement: (i) even label frequency with zero odd moments, the
exact weighted ternary occupancy problem on 127 pairs plus -1; or (ii) a
nonzero odd-moment frequency, using signed recurrence/orbit cancellation. A
route is admissible only if it gives a pointwise contribution below
R=B*|G|-A, or an explicit fibre above B*. Preserve phases; do not replace the
pointwise target by global L1 or averaging.

The exhaustive mu32 regression is known: all 7,888,725 full fibres are
singletons and the projected (p2,p4,p6,label mod 16) maximum is 4. Any claimed
universal heavy-fibre mechanism must explain this model.

Return MATCHEDFAMILY_LE_BSTAR, HEAVY_FIBRE_GT_BSTAR,
FOURIER_COMPLEMENT_OBSTRUCTION, or NO_FRONTIER_CHANGE.

## Aristotle(parallel): collective RS load gate

The downstream implication is audited:
sum_R mu_R Phi(R) <= T=19559298652205332 implies #Bad <= B*.

Do not try to prove that every individual official witness is a union of fixed
coset blocks; this is false. For every E of size e, one can choose q in V_E
and s1 outside V_E and set s0=q-gamma0*s1, producing an official rank-two
witness with that arbitrary E.

The only valid grading target is collective: if the load exceeds T, prove that
the many witnesses for one common received pair force a common smooth/coset
normal form, or derive alternative list bounds L,M with L*M <= T. If only a
total block count a+b<=54 is obtained, use the exact worst-case count
binom(32,16)binom(16,8)=7,735,904,619,300, not merely the (17,9) value.

The proof must use captured multiplicities, common rank-two syndrome data,
split locators/root divisors and official nondegeneracy. Raw support counts,
arbitrary kernels, and injective charging alone are closed.

Return HIGH_LOAD_GRADING_PROVED, ALTERNATIVE_LOAD_BOUND,
HIGH_LOAD_GRADING_COUNTEREXAMPLE, or RS_GLOBAL_LOAD_OBSTRUCTION.

For either branch separate PROVED, COMPUTATIONALLY_CHECKED, CONDITIONAL and
HEURISTIC claims. Any Lean addition must be additive, sorry-free,
axiom-audited, and outside submission roots.
