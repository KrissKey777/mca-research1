# S16.73 — wynik mostu P0 + S16.72 cut + L4

Status: `BRIDGE_SURVIVES_BOUNDED_SWEEP` (P0.2, 9 modeli, 0 mismatchów).
`GRAND_MCA_SAFE` nie zgłoszone. Zero `sorry`, zero buildu, zero merge'u paczek.

## 1. P0.1 — IsMCA: literalna ekstrakcja (EXTERNAL_SOURCE)

`IsMCA` nie jest zdefiniowane w POBIERZ1. Łańcuch:
`McaBadSet.lean` → `GrandMcaReduction.lean` →
`ArkLib.Data.CodingTheory.ProximityGap.GrandChallenges` (rev `e65197892`).
W paczce banked jest natomiast pełny adapter support-exact
(`ArkLibMcaResidualBridge.lean`):
`IsMCA γ ↔ ∃ S, McaEventOn γ S` (`Iff.rfl`, nic nie gubi),
`McaEventOn γ S ↔ (|S|≥n(1−δ) ∧ ∃c∈C ∀i∈S: U₀+γU₁=c ∧ ¬Invisible C U S)`,
`McaEventOn γ S ↔ ∃ w: witness, challenge=γ ∧ support=S`,
`γ ∈ badSet ↔ ∃ w: challenge=γ`. Świadek `BadWitness`: support/challenge/
codeword/heavy/agree/fail; `challenge_eq` (support wyznacza challenge),
`challenge_mem_badSet`, `codeword_eqOn`, `ofBadSupport` (support→świadek).
`Invisible C U S := dualSupported C S ≤ ker (syndromeMap U)`,
`invisible_iff_forall_explained` (oba wiersze wyjaśnione),
`Explains` (para codewordów), `glueClass` (klasa kanoniczna).
To jest język, w którym żyją konsumenci P1 — most musi lądować dokładnie tu.

## 2. P0.2 — falsyfikator IsBad vs IsMCA-proxy: SURVIVES (9 modeli)

`analysis/p02_bridge_falsifier.py` (deterministyczny, exact, dual Gauss+minor):
proxy = heaviness + interpolant + failure clause (kształt `McaEventOn`).
Wynik: GF(5)k2, GF(7)k2×2, GF(11)k3×2, GF(13)k3×2, GF(13)k4×2 —
**0 mismatchów**, bad>0 w 7/9 modeli (GF(13)k3 zdegenerowany, 0/13 —
zaznaczone, nie ukrywane). Receipt: `analysis/p02_bridge_falsifier.json/.txt`.
Ograniczenie uczciwe: proxy ≠ pełne `IsMCA` (brak `LinearCode.projectedCodeSubmod`
vs `F[X]`-świadek w małych modelach); werdykt to survival, nie dowód.

## 3. S16.72 cut — potwierdzony po mojej stronie, z sygnaturami

Lokalnie (`BadSupportCoveringTriple.lean`):
`Covering C S := shortening C Sᶜ = ⊥` + `covering_iff_forall_eq` (unikatność
interpolacji) + `card_le_or_exists_obstruction`: `chal ≤ e+1 ∨ ∃ triple:
¬Covering ∩∩ ∧ ∃ z≠0 ∈ shortening (∩)ᶜ, wt≤3e`.
Pakiet (`ArkLibMcaResidualBridge.lean`):
`witness_cover_forces_nonModular` (hyp: `w₁.support ⊆ w₂.support ∪ w₃.support`
→ ¬Invisible ∪∪ ∧ klasa≠0 ∧ ¬ModularPair ∧ defect>0 ∧ strict submodularity ∧
dual witness ∧ terminal/dim-descent) oraz `witness_cover_residual_descent`
(hyp: ta sama inkluzja → Invisible w shortened + codeRank ∩∩=0 + dim-descent).
Cut: lokalna obstruckja to `z≠0` w shortening dopełnienia **potrójnego
przecięcia**; pakiet konsumuje inkluzję **S₁ ⊆ S₂∪S₃**. Lokalny triple daje
`wt≤3e=2936832 > N=2097152` → WEAK/DEAD dla tego wiersza bez wzmocnienia do
`jointDefect≤69632`. Pierwsza brakująca krawędź: support-covered triple →
residual descent albo support-expansion → rank/cover price. Status S16.72
podtrzymany: `SOURCE_GAP — SUPPORT_EXPANSION_OR_RESIDUAL_DESCENT`.

## 4. L4 — sharp vs non-sharp: ROZSTRZYGNIĘTE

`deployed_safe_of_residualRank_le_nine` używa **non-sharp**
(`card_le_of_residualSystemOn`): cap = `1048577⁹·981105 / 67473⁹` =
**51873822725733337** (fits, slack 2.23e17). Liczba 5178081604 z prozy S16.69
nie pochodzi z żadnego lematu paczki (przeszukane; sharp-r12/r14 dają
2.7e17/2.4e17/4.8e16 — żaden nie daje 5.18e9 ani nie dotyczy rangi 9).
Wniosek: w cenach używać 51873822725733337; prozę 5.18e9 traktować jako
błędny cytat do czasu wskazania lematu-źródła. Oba mieszczą się w Bstar,
więc tor P1 stoi, ale receipt musi nosić liczbę z lematu.

## 5. P0.3 — spec adaptera (statements-only, w raporcie z poprzedniej tury)

`mca_work/S1660_BucketDualAdapter.lean` + `mca_work/S1669_BridgeSpec.lean`
(`PROPOSED_SOURCE_ONLY`, nie w submission root). Brakujące przesłanki typów:
świadek `F[X]` vs `w∈C`; domena `↥D` vs `ι`; próg `D.card≤S.card+e` (ℕ) vs
`|agree|≥n(1−δ)` (ℝ); podłogi `a∈{1116048,1118208}` vs `69630+K`; `Kz=1048575`
(wymaga C=RS+MDS, brak lokalnego producenta).

## 6. Downstream i next step

```
[P0.2 survival, 9 modeli] → P0.3 adapter (statements) → [BRAK dowodu: cache]
  → five-flat consumer (PACKAGE_PRESENT_UNREPLAYED) → #badSet ≤ Bstar ✔ (def eq)
S16.72: [BRAK] support-covered triple → residual descent (SOURCE_GAP)
```

Jeden next step: kill-test 5-flat-cover na modelach z **wplantowaną**
przesłanką triple-`>Kz` (aktywny analog D7-bucketu); równolegle decyzja
o cache Mathlib (bez niej brak targeted build — `Mathlib.olean` nie istnieje
nigdzie na dysku). DKT/Jeronimo/exponent≥3: DEAD. UltraBall: filtr 4627/74384.
D7: tylko full-rank-3 [11207,124777]. `matchingLike`: odrzucone (absent).
