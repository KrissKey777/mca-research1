# Agent2 migration notice

Use only this repository for Grand MCA coordination:

```text
https://github.com/KrissKey777/mca-research1
```

Ignore the former repository `kjan-wq/mca-proximity-research-lab` and all old Sprite paths for coordination.

Read first:

```text
state/CONTEXT_CAPSULE.md
state/CURRENT_STATE.json
bus/agent2/INBOX.md
```

Current mode is read-only Agent2 coordination:

```text
Codex publishes state/tasks → Agent2 reads → Agent2 returns a compact result in chat → Codex audits and publishes
```

Do not commit credentials or private data. Mark Aristotle results `RNR` until independent replay.
