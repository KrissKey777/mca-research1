# Research loop activation

The loop is now Codex-first and active. The main Codex integrator task was instructed to run a complete research cycle; the two review tasks remain the integrator and adversarial lanes. Local computation and Codex research continue while Aristotle works.

Aristotle dispatch is intentionally outside the research cycle: it is serialized and requires the final Codex/Luna review of a compact packet. No Paseo or other orchestration layer is part of the active design.

The old `H-MY-TASK1` residual-dimension claim is marked `BLOCKED`, because the latest glue-domain analysis does not supply the required choice-independent official adapter. This prevents the loop from repeatedly spending cycles on a known semantic gap.

Current health caveat: the local shell sees the parallel Aristotle project configuration, but not the separate new-account key, and no local model endpoint is configured. Codex threads therefore perform the active research; no Aristotle submission is attempted by this activation.
