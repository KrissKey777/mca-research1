# Grand MCA — top 3 MathAgent tasks

Ranking is by possible impact on the deployed theorem, not by ease.

## 1. Split-locator incidence theorem — highest priority

Target the exact deployed gate
\[
\#\{\gamma:\exists Q\mid X^n-1,\ \deg Q=e,\ Q\text{ monic},
\ (A+\gamma B)Q=0,\ \text{nondegenerate}\}\le B^*.
\]
Use the direct monic locator \(Q_E(X)=\prod_{x\in E}(X-x)\), the equivalent reciprocal key equation only with its correct convention, and the nonlinear condition \(Q\mid X^n-1\). Seek either a proof with an explicit bound \(\le B^*\), a deployed counterexample, or a rigorously isolated obstruction; do not claim progress from the unconstrained kernel, linear annihilators, pairwise overlap, or generic support counting.

## 2. Common-parent identity → residual quotient bridge

Starting from the proved triple common-parent/divided-difference identity, determine whether it induces a nontrivial relation in the existing residual quotient object (deviation class, absorbed \(C\times C\), canonical residual class). Check well-definedness under changing codeword witnesses and preservation of support/noncontainment data; reject the route if the quotient identity is merely \([d]=0\) because \(d\in C\) tautologically. A successful result must produce a new restriction on residual challenges or a quantitative consequence, not just repackage the local identity.

## 3. Aperiodic \(m=8\) construction branch

Continue the corrected finite-field/Prouhet–Tarry–Escott analysis without using the false \(\operatorname{ord}(q)=8\) closure or inversion as a symmetry. Classify the remaining genuinely aperiodic trades satisfying the exact coefficient/resultant conditions, and determine whether they can amplify a MatchedFamily fibre beyond \(B^*\); an exact impossibility theorem or an explicit large family are both valuable. Keep this branch separate from the universal SAFE split-locator theorem and do not infer a Grand MCA conclusion from small models.

## Evidence rules

Every output must label PROVED, exact finite computation, conditional, or open; preserve direct-vs-reciprocal locator conventions; and state the first missing implication when a route fails. No Lean/source search is required for MathAgent; use exact symbolic algebra and reproducible Python computations if useful.
