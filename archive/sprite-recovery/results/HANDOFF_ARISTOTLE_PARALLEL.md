# HANDOFF ARISTOTLE PARALLEL

## Exact RelativeConflictAgreement target

Assume the official same-support predicate and #Bad > B*. Prove that there is
one projective pencil P whose quotient-visible certificate family Q(P) satisfies

  |Q(P)| >= 302754427

after exact losses from exceptional collinear covectors, chart choices, and
fibre multiplicities.

Do not replace Q(P) by raw close directions.

## Exact numerical thresholds

Current deployed ambiguity dichotomy gives:
  X_max = 129056129
for the number of exceptional ambiguous directions.

If conflicts are distributed over the N=p^2+p+1 projective directions, then
  max E_lambda >= ceil(E_conf/N).
To force E_lambda >= T, it is necessary/sufficient by pigeonhole that
  E_conf >= (T-1)N+1.

For the 3-chart quotient theorem use T=302754427.

## Exact incidence identities

N=p^2+p+1=4539909905758289923.
Every line has p+1=2130706434 points.
Every point lies on p+1 lines.
Two points determine one line.
For the incidence matrix A:
  A A^T = p I + J.
Levi graph spectrum: ±(p+1), ±sqrt(p).

## Best theorem patterns

1. Finite-projective-plane incidence/spectral mixing: exact and cheap, but only
   gives average/variance until combined with conflict structure.
2. Local-to-global agreement testing for linear maps on overlapping subspaces:
   use only as a pattern unless exact hypotheses are supplied.
3. Gain/signed-graph balanced-subgraph/frustration bounds: useful language for
   conflict edges, but generic bounds have weak semantic match and need a
   gain-valued adaptation.

The key missing theorem is concentration into quotient-visible witnesses, not
merely a bound on the number of close directions.

