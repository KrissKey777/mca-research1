# ProjectiveIncidenceAdapter

Unproved adapter specification for PG(2,p). Required exact lemmas: card points = p^2+p+1; card lines = p^2+p+1; each line has p+1 points; each point lies on p+1 lines; unique line through two points; A*A^T = p I + J; Levi spectrum if needed. No RS-specific theorem is included.

