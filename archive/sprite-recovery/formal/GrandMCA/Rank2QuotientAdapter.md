# Rank2QuotientAdapter

Unproved adapter specification. Given a rank-2 linear map R -> F_p^2 and an exact-support certificate, normalize it to a canonical PG(1,p) parameterization, preserving fibre/cardinality and the certificate. Required theorem must expose the exact loss M of charts/fibres; for M charts use L_max(M)=floor(sqrt(B*/M)).

