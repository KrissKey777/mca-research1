# ConflictCountingAdapter

Unproved adapter specification. Reuse exact finite-set collision, affine-fibre, and two-mode pruning lemmas only after local replay. Required output: a theorem converting a certified conflict-edge set into E_conf = sum_lambda E_lambda and then max_lambda E_lambda >= ceil(E_conf/N), with all finite-set coercions explicit.

